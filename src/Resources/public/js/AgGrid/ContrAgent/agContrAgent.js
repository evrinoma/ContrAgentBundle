import Vue from 'vue';
import AgContrAgent from './AgContrAgent';

/* eslint-disable no-new */
new Vue({
    el: '#agContrAgent',
    template: '<agContrAgent/>',
    components: { AgContrAgent }
});
